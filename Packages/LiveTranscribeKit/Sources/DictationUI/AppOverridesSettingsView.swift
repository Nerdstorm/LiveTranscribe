import SwiftUI

/// Settings › Apps: per-app insertion overrides. (Placeholder, replaced by the UI slice.)
public struct AppOverridesSettingsView: View {
    let context: DictationUIContext

    public init(context: DictationUIContext) { self.context = context }

    public var body: some View {
        Text("Apps")
    }
}
