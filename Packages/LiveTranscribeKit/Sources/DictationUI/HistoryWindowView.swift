import SwiftUI

/// The dictation history window. (Placeholder, replaced by the UI slice.)
public struct HistoryWindowView: View {
    let context: DictationUIContext

    public init(context: DictationUIContext) { self.context = context }

    public var body: some View {
        Text("History")
    }
}
