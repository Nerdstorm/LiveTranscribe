import SwiftUI

/// Settings › Snippets. (Placeholder, replaced by the UI slice.)
public struct SnippetsSettingsView: View {
    let context: DictationUIContext

    public init(context: DictationUIContext) { self.context = context }

    public var body: some View {
        Text("Snippets")
    }
}
