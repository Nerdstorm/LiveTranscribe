import SwiftUI

/// Settings › Vocabulary. (Placeholder, replaced by the UI slice.)
public struct VocabularySettingsView: View {
    let context: DictationUIContext

    public init(context: DictationUIContext) { self.context = context }

    public var body: some View {
        Text("Vocabulary")
    }
}
